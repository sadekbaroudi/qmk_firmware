{
    "$schema": "../src/schemas/fp_build.jsonschema",

    "options": [
        {
            "type": "single",
            "name": "ENCODER_ENABLE",
            "user_input": "Do you have a rotary encoder?",
            "default": "no"
        }
    ]
}
