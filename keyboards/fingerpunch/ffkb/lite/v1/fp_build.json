{
    "$schema": "../../../src/schemas/fp_build.jsonschema",

    "options": [
        {
            "type": "single",
            "name": "FP_INDICATOR_LED_ENABLE",
            "user_input": "Do you have indicator LEDs?",
            "default": "no"
        }
    ]
}
